services:
  iptables-web-ui:
    container_name: iptables-web-ui
    image: iptables-web-ui:latest
    network_mode: host
    cap_add:
     - NET_ADMIN
    environment:
      - TZ=<timezone>
      - WEBUI_PASSWORD=<my_password>
      - WEBUI_JWT_KEY=<long_random_string>
    restart: unless-stopped
